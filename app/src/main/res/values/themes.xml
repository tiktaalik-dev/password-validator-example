<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PasswordValidator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PasswordValidator" parent="Base.Theme.PasswordValidator" />

    <style name="actionBarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:elevation">4dp</item>
    </style>
</resources>